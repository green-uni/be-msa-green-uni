<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.green.core.application.lecture.mapper.LectureMapper">

    <!-- 공통 schedule 컬럼 -->
    <sql id="scheduleColumns">
        ls.day_of_week  AS dayOfWeek,
        ls.start_period AS startPeriod,
        ls.end_period   AS endPeriod,
        lr.building,
        lr.room         AS room
    </sql>

    <!-- 공통 schedule JOIN -->
    <sql id="scheduleJoins">
        LEFT JOIN lecture_schedule ls ON l.lecture_id = ls.lecture_id
        LEFT JOIN classroom lr        ON ls.room_id = lr.room_id
    </sql>

    <!--schedules가 List<ScheduleRes>로 보여줘서 중첩리스트 사용하기 위함-->
    <resultMap id="myLectureListResMap" type="com.green.core.application.lecture.model.MyLectureListRes">
        <id property="lectureId" column="lectureId"/>
        <result property="memberCode" column="memberCode"/>
        <result property="status" column="status"/>
        <result property="lectureType" column="lectureType"/>
        <result property="lectureName" column="lectureName"/>
        <result property="majorName" column="majorName"/>
        <result property="proName" column="proName"/>
        <result property="credit" column="credit"/>
        <result property="academicYear" column="academicYear"/>
        <result property="year" column="year"/>
        <result property="semester" column="semester"/>
        <collection property="schedules" ofType="com.green.core.application.lecture.model.MyLectureListRes$ScheduleRes">
            <result property="dayOfWeek" column="dayOfWeek"/>
            <result property="startPeriod" column="startPeriod"/>
            <result property="endPeriod" column="endPeriod"/>
            <result property="building" column="building"/>
            <result property="room" column="room"/>
        </collection>
    </resultMap>

    <resultMap id="lectureListResMap" type="com.green.core.application.lecture.model.LectureListRes">
        <id property="lectureId" column="lectureId"/>
        <result property="memberCode" column="memberCode"/>
        <result property="lectureType" column="lectureType"/>
        <result property="lectureName" column="lectureName"/>
        <result property="majorName" column="majorName"/>
        <result property="proName" column="proName"/>
        <result property="credit" column="credit"/>
        <result property="academicYear" column="academicYear"/>
        <result property="year" column="year"/>
        <result property="semester" column="semester"/>
        <collection property="schedules" ofType="com.green.core.application.lecture.model.LectureListRes$ScheduleRes">
            <result property="dayOfWeek" column="dayOfWeek"/>
            <result property="startPeriod" column="startPeriod"/>
            <result property="endPeriod" column="endPeriod"/>
            <result property="building" column="building"/>
            <result property="room" column="room"/>
        </collection>
    </resultMap>

    <!-- base: status, maxStd 없음 (학생용) -->
    <resultMap id="lectureDetailResMap" type="com.green.core.application.lecture.model.LectureDetailRes">
        <id property="lectureId" column="lectureId"/>
        <result property="memberCode" column="memberCode"/>
        <result property="majorId" column="majorId"/>
        <result property="majorName" column="majorName"/>
        <result property="proName" column="proName"/>
        <result property="year" column="year"/>
        <result property="semester" column="semester"/>
        <result property="lectureName" column="lectureName"/>
        <result property="credit" column="credit"/>
        <result property="lectureType" column="lectureType"/>
        <result property="refBooks" column="refBooks"/>
        <result property="goal" column="goal"/>
        <result property="weeklyPlan" column="weeklyPlan"/>
        <result property="academicYear" column="academicYear"/>
        <result property="startDate" column="startDate"/>
        <result property="endDate" column="endDate"/>
        <collection property="schedules" ofType="com.green.core.application.lecture.model.LectureDetailRes$ScheduleRes">
            <result property="dayOfWeek" column="dayOfWeek"/>
            <result property="startPeriod" column="startPeriod"/>
            <result property="endPeriod" column="endPeriod"/>
            <result property="building" column="building"/>
            <result property="room" column="room"/>
        </collection>
    </resultMap>

    <!-- 교수용: base 상속 + status, maxStd 추가 -->
    <resultMap id="proAdmLectureDetailResMap" type="com.green.core.application.lecture.model.LectureDetailRes" extends="lectureDetailResMap">
        <result property="memberCode" column="memberCode"/>
        <result property="status" column="status"/>
        <result property="maxStd" column="maxStd"/>
        <result property="rejectionReason" column="rejectionReason"/>
        <result property="rejectionAt" column="rejectionAt"/>
        <result property="rejectorCode" column="rejectorCode"/>
        <result property="rejectorName" column="rejectorName"/>
        <result property="approverCode" column="approverCode"/>
        <result property="approverName" column="approverName"/>
        <result property="cancelReason" column="cancelReason"/>
        <result property="cancelAt" column="cancelAt"/>
    </resultMap>


    <!--연도쿼리-->
    <select id="findLectureYears" resultType="int">
        SELECT DISTINCT year
        FROM lecture
        WHERE status = 'APPROVED'
        AND is_del = false
        AND year != 9999
        ORDER BY year DESC
    </select>

    <select id="findStudentLectureYears" resultType="int">
        SELECT DISTINCT l.year
        FROM course c
        JOIN lecture l ON c.lecture_id = l.lecture_id
        WHERE c.student_code = #{memberCode}
        AND c.is_del = false
        ORDER BY l.year DESC
    </select>

    <select id="findProfessorLectureYears" resultType="int">
        SELECT DISTINCT year
        FROM lecture
        WHERE member_code = #{memberCode}
        AND is_del = false
        ORDER BY year DESC
    </select>


    <!-- LEC-03 관리자: 승인관리 목록 -->
    <select id="findAdminLectures" resultMap="myLectureListResMap">
        SELECT
        l.lecture_id    AS lectureId,
        l.member_code   AS memberCode,
        l.status,
        l.lecture_type  AS lectureType,
        l.lecture_name  AS lectureName,
        mj.name         AS majorName,
        pc.name         AS proName,
        l.credit,
        l.academic_year AS academicYear,
        l.year,
        l.semester,
        <include refid="scheduleColumns"/>
        FROM (
        SELECT DISTINCT l.lecture_id
        FROM lecture l
        WHERE l.is_del = false
        <if test="status != null">AND l.status = #{status}</if>
        <if test="lectureName != null and lectureName != ''">
            AND l.lecture_name LIKE CONCAT('%', #{lectureName}, '%')
        </if>
        <if test="year != null">AND l.year = #{year}</if>
        <if test="semester != null">AND l.semester = #{semester}</if>
        ORDER BY l.lecture_id DESC
        LIMIT #{size} OFFSET #{offset}
        ) paged
        JOIN lecture l ON l.lecture_id = paged.lecture_id
        LEFT JOIN professor_cache pc ON l.member_code = pc.member_code
        LEFT JOIN major mj ON l.major_id = mj.major_id
        LEFT JOIN lecture_schedule ls ON l.lecture_id = ls.lecture_id
        LEFT JOIN classroom lr ON ls.room_id = lr.room_id
        ORDER BY l.lecture_id DESC
    </select>

    <select id="countAdminLectures" resultType="long">
        SELECT COUNT(*)
        FROM lecture
        WHERE is_del = false
        <if test="status != null">AND status = #{status}</if>
        <if test="lectureName != null and lectureName != ''">
            AND lecture_name LIKE CONCAT('%', #{lectureName}, '%')
        </if>
        <if test="year != null">AND year = #{year}</if>
        <if test="semester != null">AND semester = #{semester}</if>
    </select>

    <!-- LEC-06 교수: 내 강의 목록 -->
    <select id="findProfessorMyLectures" resultMap="myLectureListResMap">
        SELECT
        l.lecture_id    AS lectureId,
        l.member_code   AS memberCode,
        l.status,
        l.lecture_type  AS lectureType,
        l.lecture_name  AS lectureName,
        mj.name         AS majorName,
        pc.name         AS proName,
        l.credit,
        l.academic_year AS academicYear,
        l.year,
        l.semester,
        <include refid="scheduleColumns"/>
        FROM (
        SELECT DISTINCT l.lecture_id
        FROM lecture l
        WHERE l.member_code = #{memberCode}
        AND l.is_del = false
        <if test="req.status != null">AND l.status = #{req.status}</if>
        <if test="req.lectureName != null and req.lectureName != ''">
            AND l.lecture_name LIKE CONCAT('%', #{req.lectureName}, '%')
        </if>
        <if test="req.year != null">AND l.year = #{req.year}</if>
        <if test="req.semester != null">AND l.semester = #{req.semester}</if>
        ORDER BY l.lecture_id DESC
        LIMIT #{req.size} OFFSET #{req.offset}
        ) paged
        JOIN lecture l ON l.lecture_id = paged.lecture_id
        LEFT JOIN professor_cache pc ON l.member_code = pc.member_code
        LEFT JOIN major mj ON l.major_id = mj.major_id
        LEFT JOIN lecture_schedule ls ON l.lecture_id = ls.lecture_id
        LEFT JOIN classroom lr ON ls.room_id = lr.room_id
        ORDER BY l.lecture_id DESC
    </select>

    <select id="countProfessorMyLectures" resultType="long">
        SELECT COUNT(*)
        FROM lecture
        WHERE member_code = #{memberCode}
        AND is_del = false
        <if test="req.status != null">AND status = #{req.status}</if>
        <if test="req.lectureName != null and req.lectureName != ''">
            AND lecture_name LIKE CONCAT('%', #{req.lectureName}, '%')
        </if>
        <if test="req.year != null">AND year = #{req.year}</if>
        <if test="req.semester != null">AND semester = #{req.semester}</if>
    </select>

    <!-- LEC-07 학생: 수강신청한 강의 -->
    <select id="findStudentMyLectures" resultMap="lectureListResMap">
        SELECT
        l.lecture_id    AS lectureId,
        l.lecture_type  AS lectureType,
        l.lecture_name  AS lectureName,
        mj.name         AS majorName,
        pc.name         AS proName,
        l.credit,
        l.academic_year AS academicYear,
        l.year,
        l.semester,
        <include refid="scheduleColumns"/>
        FROM (
        SELECT DISTINCT l.lecture_id
        FROM course c
        JOIN lecture l ON c.lecture_id = l.lecture_id
        WHERE c.student_code = #{memberCode}
        AND l.status = 'APPROVED'
        <if test="req.year != null">AND l.year = #{req.year}</if>
        <if test="req.semester != null">AND l.semester = #{req.semester}</if>
        <choose>
            <when test="req.createdBefore != null">
                AND c.created_at &lt; #{req.createdBefore}
                AND (c.is_del = false OR c.deleted_at &gt;= #{req.createdBefore})
            </when>
            <otherwise>
                AND c.is_del = false
            </otherwise>
        </choose>
        ORDER BY l.lecture_id DESC
        LIMIT #{req.size} OFFSET #{req.offset}
        ) paged
        JOIN lecture l                ON l.lecture_id = paged.lecture_id
        LEFT JOIN professor_cache pc  ON l.member_code = pc.member_code
        LEFT JOIN major mj            ON l.major_id = mj.major_id
        <include refid="scheduleJoins"/>
        ORDER BY l.lecture_id DESC
    </select>

    <select id="countStudentMyLectures" resultType="long">
        SELECT COUNT(DISTINCT l.lecture_id)
        FROM course c
        JOIN lecture l ON c.lecture_id = l.lecture_id
        WHERE c.student_code = #{memberCode}
        AND l.status = 'APPROVED'
        <if test="req.year != null">AND l.year = #{req.year}</if>
        <if test="req.semester != null">AND l.semester = #{req.semester}</if>
        <choose>
            <when test="req.createdBefore != null">
                AND c.created_at &lt; #{req.createdBefore}
                AND (c.is_del = false OR c.deleted_at &gt;= #{req.createdBefore})
            </when>
            <otherwise>
                AND c.is_del = false
            </otherwise>
        </choose>
    </select>

    <!-- LEC-08 전체 강의 목록 -->
    <select id="findAllLectures" resultMap="lectureListResMap">
        SELECT
        l.lecture_id    AS lectureId,
        l.lecture_type  AS lectureType,
        l.lecture_name  AS lectureName,
        mj.name         AS majorName,
        pc.name         AS proName,
        l.credit,
        l.academic_year AS academicYear,
        l.year,
        l.semester,
        <include refid="scheduleColumns"/>
        FROM (
        SELECT DISTINCT l.lecture_id
        FROM lecture l
        LEFT JOIN professor_cache pc ON l.member_code = pc.member_code
        WHERE l.status = 'APPROVED'
        <if test="year != null">AND l.year = #{year}</if>
        <if test="year == null">AND l.year != 9999</if>
        <if test="(lectureName != null and lectureName != '') or (proName != null and proName != '')">
            AND (
            <if test="lectureName != null and lectureName != ''">
                l.lecture_name LIKE CONCAT('%', #{lectureName}, '%')
            </if>
            <if test="proName != null and proName != ''">
                OR pc.name LIKE CONCAT('%', #{proName}, '%')
            </if>
            )
        </if>
        <if test="majorId != null">AND l.major_id = #{majorId}</if>
        <if test="semester != null">AND l.semester = #{semester}</if>
        ORDER BY l.lecture_id DESC
        LIMIT #{size} OFFSET #{offset}
        ) paged
        JOIN lecture l                ON l.lecture_id = paged.lecture_id
        LEFT JOIN professor_cache pc  ON l.member_code = pc.member_code
        LEFT JOIN major mj            ON l.major_id = mj.major_id
        <include refid="scheduleJoins"/>
        ORDER BY l.lecture_id DESC
    </select>

    <select id="countAllLectures" resultType="long">
        SELECT COUNT(DISTINCT l.lecture_id)
        FROM lecture l
        LEFT JOIN professor_cache pc ON l.member_code = pc.member_code
        WHERE l.status = 'APPROVED'
        <if test="year != null">AND l.year = #{year}</if>
        <if test="year == null">AND l.year != 9999</if>
        <if test="(lectureName != null and lectureName != '') or (proName != null and proName != '')">
            AND (
            <if test="lectureName != null and lectureName != ''">
                l.lecture_name LIKE CONCAT('%', #{lectureName}, '%')
            </if>
            <if test="proName != null and proName != ''">
                OR pc.name LIKE CONCAT('%', #{proName}, '%')
            </if>
            )
        </if>
        <if test="majorId != null">AND l.major_id = #{majorId}</if>
        <if test="semester != null">AND l.semester = #{semester}</if>
    </select>

    <!-- LEC-09 교수, 관리자: 강의 상세 조회 -->
    <select id="findProAdmLectureDetail" resultMap="proAdmLectureDetailResMap">
        SELECT
        l.lecture_id    AS lectureId,
        l.major_id      AS majorId,
        mj.name         AS majorName,
        pc.name         AS proName,
        l.year,
        l.semester,
        l.lecture_name  AS lectureName,
        l.credit,
        l.lecture_type  AS lectureType,
        l.ref_books     AS refBooks,
        l.goal,
        l.weekly_plan   AS weeklyPlan,
        l.academic_year AS academicYear,
        l.max_std       AS maxStd,
        l.status,
        l.start_date    AS startDate,
        l.end_date      AS endDate,
        l.member_code   AS memberCode,
        lrj.reason        AS rejectionReason,
        lrj.created_at    AS rejectionAt,
        lrj.updator_code  AS rejectorCode,
        lrj.updator_name  AS rejectorName,
        l.approver_code   AS approverCode,
        l.approver_name   AS approverName,
        lh.change_reason  AS cancelReason,
        lh.created_at     AS cancelAt,
        <include refid="scheduleColumns"/>
        FROM lecture l
        LEFT JOIN major mj            ON l.major_id = mj.major_id
        LEFT JOIN professor_cache pc  ON l.member_code = pc.member_code
        LEFT JOIN lecture_rejection lrj ON lrj.rejection_id = (
            SELECT rejection_id FROM lecture_rejection
            WHERE lecture_id = l.lecture_id
            ORDER BY created_at DESC
            LIMIT 1
        )
        LEFT JOIN lecture_history lh ON lh.history_id = (
            SELECT history_id FROM lecture_history
            WHERE lecture_id = l.lecture_id
            AND change_type = 'CANCEL'
            ORDER BY created_at DESC
            LIMIT 1
        )
        <include refid="scheduleJoins"/>
        WHERE l.lecture_id = #{lectureId}
        AND l.is_del = false
    </select>

    <!-- LEC-10 학생: 강의 상세 조회 -->
    <select id="findStudentLectureDetail" resultMap="lectureDetailResMap">
        SELECT
        l.lecture_id    AS lectureId,
        l.member_code   AS memberCode,
        l.major_id      AS majorId,
        mj.name         AS majorName,
        pc.name         AS proName,
        l.year,
        l.semester,
        l.lecture_name  AS lectureName,
        l.credit,
        l.lecture_type  AS lectureType,
        l.ref_books     AS refBooks,
        l.goal,
        l.weekly_plan   AS weeklyPlan,
        l.academic_year AS academicYear,
        l.start_date    AS startDate,
        l.end_date      AS endDate,
        <include refid="scheduleColumns"/>
        FROM lecture l
        LEFT JOIN major mj            ON l.major_id = mj.major_id
        LEFT JOIN professor_cache pc  ON l.member_code = pc.member_code
        <include refid="scheduleJoins"/>
        WHERE l.lecture_id = #{lectureId}
    </select>

    <!-- DASH 교수 시간표 -->
    <select id="findProfessorTimetable" resultMap="myLectureListResMap">
        SELECT
        l.lecture_id    AS lectureId,
        l.member_code   AS memberCode,
        l.lecture_name  AS lectureName,
        pc.name         AS proName,
        l.year,
        l.semester,
        <include refid="scheduleColumns"/>
        FROM lecture l
        LEFT JOIN professor_cache pc ON l.member_code = pc.member_code
        <include refid="scheduleJoins"/>
        WHERE l.member_code = #{memberCode}
        AND l.status = 'APPROVED'
        AND l.is_del = false
        <if test="year != null">AND l.year = #{year}</if>
        <if test="semester != null">AND l.semester = #{semester}</if>
        ORDER BY l.lecture_id
    </select>

    <!-- DASH 학생 시간표 -->
    <select id="findStudentTimetable" resultMap="lectureListResMap">
        SELECT
        l.lecture_id    AS lectureId,
        l.lecture_name  AS lectureName,
        pc.name         AS proName,
        l.year,
        l.semester,
        <include refid="scheduleColumns"/>
        FROM course c
        JOIN lecture l               ON c.lecture_id = l.lecture_id
        LEFT JOIN professor_cache pc ON l.member_code = pc.member_code
        <include refid="scheduleJoins"/>
        WHERE c.student_code = #{memberCode}
        AND c.is_del = false
        AND l.status = 'APPROVED'
        <if test="year != null">AND l.year = #{year}</if>
        <if test="semester != null">AND l.semester = #{semester}</if>
        ORDER BY l.lecture_id
    </select>

    <!-- DASH 교수: 오늘 강의 목록 -->
    <select id="findTodayLectures" resultType="com.green.core.application.lecture.model.TodayLectureRes">
        SELECT
        l.lecture_id        AS lectureId,
        l.lecture_name      AS lectureName,
        lr.building,
        lr.room,
        ls.start_period     AS startPeriod,
        ls.end_period       AS endPeriod,
        ats.attendsession_id AS attendSessionId,
        ats.is_active       AS isActive
        FROM lecture l
        JOIN lecture_schedule ls    ON l.lecture_id = ls.lecture_id
        JOIN classroom lr           ON ls.room_id = lr.room_id
        LEFT JOIN attendance_session ats
        ON ats.lecture_id = l.lecture_id
        AND DATE(ats.class_date) = CURDATE()
        WHERE l.member_code = #{memberCode}
        AND l.status = 'APPROVED'
        AND l.is_del = false
        AND ls.day_of_week = #{dayOfWeek}
        AND l.year = #{year}
        AND l.semester = #{semester}
        ORDER BY ls.start_period
    </select>
</mapper>
